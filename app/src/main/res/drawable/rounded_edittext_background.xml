<!-- rounded_edittext_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="20dp" /> <!-- Adjust the radius to change the roundness -->
    <solid android:color="@android:color/darker_gray" /> <!-- Background color of the EditText -->
    <stroke

        android:width="1dp"
        android:color="@android:color/darker_gray" /> <!-- Border color and width -->
</shape>
